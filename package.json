{
  "name": "orun-vpn",
  "version": "0.1.0",
  "private": true,
  "description": "VPN pessoal do ecossistema Orun OS (wg-easy + Unbound/blocklist)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=20"
  }
}