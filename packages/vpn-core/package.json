{
  "name": "@orun/vpn-core",
  "version": "0.1.0",
  "description": "Schema, interfaces e orquestração base do Orun VPN (wg-easy por trás)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
