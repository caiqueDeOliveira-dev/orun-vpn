server:
  interface: 0.0.0.0
  port: 53
  do-ip4: yes
  do-ip6: no
  access-control: 10.8.0.0/24 allow
  hide-identity: yes
  hide-version: yes
  # DNS-over-TLS pro upstream — não vaza consultas em plaintext
  tls-cert-bundle: /etc/ssl/certs/ca-certificates.crt

forward-zone:
  name: "."
  forward-tls-upstream: yes
  forward-addr: 1.1.1.1@853#cloudflare-dns.com
  forward-addr: 1.0.0.1@853#cloudflare-dns.com

# Blocklist via RPZ (Response Policy Zone) — mecanismo nativo do Unbound
# desde a 1.16, não um "include" de lista solta. Fonte real confirmada:
# https://github.com/hagezi/dns-blocklists/blob/main/rpz/pro.txt
#
# ⚠️ GAP NÃO VERIFICADO: RPZ no Unbound depende da flag de build
# `--enable-rpz`. Não consegui rodar o container `mvance/unbound` de
# verdade neste ambiente (Docker não disponível no sandbox) pra confirmar
# se essa imagem específica foi compilada com RPZ habilitado. Testar com
# `unbound-checkconf` e um `docker compose up` real antes de confiar nisso.
rpz:
  name: "hagezi.pro.rpz"
  zonefile: "/opt/unbound/etc/unbound/hagezi-pro.rpz"
  rpz-log: yes
